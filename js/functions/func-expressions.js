// save (and use) function as a variable
/* const square = function (num) {
  return num * num;
};
console.log(square(10)); // 100 */

// save (and use) function as a variable
const square = function (num) {
  return Math.pow(num, 2);
};
console.log(square(10));
